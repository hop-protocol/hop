select source_chain_slug, sum(amount_usd) as USD, date_trunc('day', to_timestamp("timestamp")) as date from transfers where "timestamp" > 1655830800 and timestamp < 1656349140 group by (source_chain_slug, date_trunc('day', to_timestamp("timestamp")))