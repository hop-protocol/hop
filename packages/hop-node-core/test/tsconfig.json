{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,

    // Explicitly defined buildinfo file to avoid conflicts with src and
    // keep concerns separated
    "tsBuildInfoFile": "./tsconfig.tsbuildinfo",
  },
  "references": [
    { "path": "../src" }
  ]
}