select sum(amount_usd) as total_usd, date_trunc('week', to_timestamp(timestamp)) as week from transfers where token = 'ETH' and to_timestamp(timestamp) > now() - INTERVAL '6 month' group by week order by week;