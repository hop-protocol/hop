select sum(amount_usd) as total_usd, date_trunc('day', to_timestamp(timestamp)) as day from transfers where to_timestamp(timestamp) > now() - INTERVAL '6 day' and token = 'USDT' group by day order by day;