select destination_chain_slug, sum(amount_usd) as total_usd, date_trunc('day', to_timestamp(timestamp)) as day from transfers where to_timestamp(timestamp) > now() - INTERVAL '6 day' group by destination_chain_slug, day order by day;