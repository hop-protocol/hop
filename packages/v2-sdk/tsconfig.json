{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",

    // Reintroduce `strict` defaults that were disabled at root
    "strictPropertyInitialization": true,
  },
  "include": [
    "src",
    "src/**/*.json" // Required to include JSON files specifically
  ]
}
