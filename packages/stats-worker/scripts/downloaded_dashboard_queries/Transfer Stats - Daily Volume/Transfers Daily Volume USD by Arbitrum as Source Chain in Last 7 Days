select sum(amount_usd) as total_usd, date_trunc('day', to_timestamp(timestamp)) as day from transfers where source_chain_slug = 'arbitrum' and to_timestamp(timestamp) > now() - INTERVAL '6 day' group by day order by day;