select sum(amount_usd) as total_usd, date_trunc('month', to_timestamp(timestamp)) as month from transfers where token = 'USDC' and to_timestamp(timestamp) > now() - INTERVAL '24 month' group by month order by month;