select integration_partner, sum(amount_usd) as USD, date_trunc('day', to_timestamp("timestamp")) as date from transfers where integration_partner IS NOT NULL and integration_partner != '' and "timestamp" > 1667894400 group by (integration_partner, date_trunc('day', to_timestamp("timestamp")))