select amount_usd, strftime('%m - %d  - %Y ', datetime(timestamp, 'unixepoch', 'utc')) as day from tvl_pool_stats where timestamp > 1623826800 and chain = 'polygon' and token = 'USDC' order by timestamp asc;