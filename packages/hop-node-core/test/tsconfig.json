{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "skipLibCheck": true,

    // Explicitly defined buildinfo file path to avoid conflicts with src
    // buildinfo file and keep concerns separated
    "tsBuildInfoFile": "./tsconfig.tsbuildinfo"
  },
  "references": [
    { "path": "../src" }
  ]
}