{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "paths": {
      "#*": ["./src/*"] // this makes jest tests work
    }
  },
  "include": [
    "src",
    "src/**/*.json" // Required to include JSON files specifically
  ]
}
