select token, sum(amount_usd) as total_usd,  strftime('%m - %d  - %Y ', datetime(timestamp, 'unixepoch', 'utc')) as day from volume_stats where datetime(timestamp, 'unixepoch', 'utc') >= date('now','start of month','-6 months') group by token, timestamp order by timestamp