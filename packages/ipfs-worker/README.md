# Hop v1 ipfs worker

> IPFS worker to upload config files to IPFS.

Note: This worker isn't used anymore.

## Development

Install dependencies

```sh
npm install
```

Build

```sh
npm run build
```
