select destination_chain_slug, sum(amount_usd) as USD, date_trunc('day', to_timestamp("timestamp")) as date from transfers where token = 'HOP' and source_chain_slug = 'gnosis' and to_timestamp(timestamp) > now() - INTERVAL '6 day' group by (destination_chain_slug, date_trunc('day', to_timestamp("timestamp")))